/**
 * Predefined application roles.
 * Single source of truth — FR-004 will extend this list when granular
 * permissions are added; UI components reference only this module.
 */
export const PREDEFINED_ROLES = ['user', 'admin']

export const ROLE_LABELS = {
  user:  'User',
  admin: 'Admin',
}
